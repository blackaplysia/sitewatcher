#!/usr/bin/python3

class IfSource:

    def __init__(self, name, resid, logger):
        self.name = name
        self.resid = resid
        self.logger = logger

    def make_link_set(self, hash, link, depth, ignores):
        return None
